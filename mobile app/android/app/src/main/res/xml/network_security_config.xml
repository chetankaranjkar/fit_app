<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- VPS testing over HTTP (no TLS). Production should use HTTPS + domain. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
